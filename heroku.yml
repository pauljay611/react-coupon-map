build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: stage
release:
  dockerfile: Dockerfile
  target: builder
  command:
    - docker-compose up -d
    - yarn db:create
run:
  web: yarn server:start
