build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: stage
run:
  web:
    command:
      - heroku git:clone -a react-coupon-map
      - docker-compose up -d
      - yarn db:create
      - yarn server:start
