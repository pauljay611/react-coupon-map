{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon ./src/server.ts",
    "build": "tsc --project ./",
    "db:init": "yarn sequelize-cli init",
    "db:model": "npx sequelize-cli db:migrate",
    "db:seeder": "npx sequelize-cli db:seed:all"
  }
}